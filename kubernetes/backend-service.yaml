apiVersion: v1
kind: Service
metadata:
  name: novapay-backend-service
spec:
  selector:
    app: novapay-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
